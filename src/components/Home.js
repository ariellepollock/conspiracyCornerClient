const Home = (props) => {
	// const { msgAlert, user } = props
	console.log('props in home', props)

	return (
		<>
			<h2>Home Page</h2>
			<p>this will be an intro page</p>
		</>
	)
}

export default Home
